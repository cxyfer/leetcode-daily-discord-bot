## 1. Sync Specs to Main

- [ ] 1.1 Archive the `init-project-specs` change to sync all 10 delta specs into `openspec/specs/`
- [ ] 1.2 Verify all 10 capability directories exist under `openspec/specs/` with their `spec.md` files

## 2. Validation

- [ ] 2.1 Run `openspec validate` to confirm all specs pass structural validation
- [ ] 2.2 Spot-check 2-3 specs against actual code behavior to confirm accuracy
